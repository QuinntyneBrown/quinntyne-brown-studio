@startuml
!include <C4/C4_Component>
LAYOUT_WITH_LEGEND()
title Components - Manage preferred vendors
Container_Boundary(frontend, "Frontend - Admin applications") {
 Component(page, "PreferredVendorsPage", "Angular", "Feature inputs, status, and results")
 Component(client, "IVendorService", "Interface and injection token", "Typed feature service boundary")
}
Container_Boundary(api, "Backend - QuinntyneBrownStudio.Api and Application") {
 Component(ctrl, "PreferredVendorController", ".NET controller", "Endpoint authorization and dispatch")
 Component(handler0, "GetPreferredVendorsHandler", "MediatR", "GetPreferredVendors")
 Component(handler1, "SavePreferredVendorHandler", "MediatR", "SavePreferredVendor")
 Component(entity, "PreferredVendor", "Domain model", "Feature state and policy")
 Component(store, "IStudioStore", "Application port", "Transactional persistence boundary")
}
Rel(page, client, "Consumes token contract")
Rel(client, ctrl, "Calls typed controller API", "HTTPS")
Rel(ctrl, handler0, "Dispatches request")
Rel(handler0, entity, "Uses state and policy")
Rel(handler0, store, "Loads or saves authorized state")
Rel(ctrl, handler1, "Dispatches request")
Rel(handler1, entity, "Uses state and policy")
Rel(handler1, store, "Loads or saves authorized state")
@enduml
