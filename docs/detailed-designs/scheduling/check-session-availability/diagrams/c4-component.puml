@startuml
!include <C4/C4_Component>
LAYOUT_WITH_LEGEND()
title Components - Check session availability
Container_Boundary(frontend, "Frontend - Marketing applications") {
 Component(page, "SessionTimingFields", "Angular", "Feature inputs, status, and results")
 Component(client, "IAvailabilityService", "Interface and injection token", "Typed feature service boundary")
}
Container_Boundary(api, "Backend - Qbs.Api and Application") {
 Component(ctrl, "AvailabilityController", ".NET controller", "Endpoint authorization and dispatch")
 Component(handler0, "GetAvailabilityHandler", "MediatR", "GetAvailability")
 Component(entity, "AvailabilityPolicy", "Domain model", "Feature state and policy")
 Component(store, "IStudioStore", "Application port", "Transactional persistence boundary")
}
Rel(page, client, "Consumes token contract")
Rel(client, ctrl, "Calls typed controller API", "HTTPS")
Rel(ctrl, handler0, "Dispatches request")
Rel(handler0, entity, "Uses state and policy")
Rel(handler0, store, "Loads or saves authorized state")
@enduml
