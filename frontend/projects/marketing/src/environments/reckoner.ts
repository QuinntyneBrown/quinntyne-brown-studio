import type { ReckonerPublicOptions } from '@qbs/application';
/** Build configuration: populate from the customer's public provisioning output. */
export const reckonerPublicOptions: ReckonerPublicOptions = { apiBaseUrl: '', publishableKey: '' };
