# @qbs/domain

Typed client-side values and API contracts.

Build with `npm run build:libs` from the frontend workspace. See [workspace instructions](../../README.md) and the component catalog for usage.
